title: Actualité des adhérents
icon: folder-open

options:
  preview: true
  status: true
  template: false
  url: true
  delete: true

pages:
  template: member-new
  # NOTE: using index field to display subpages
  hide: true

files: false

fields:
  _list:
    extends: _list
    fields:

      categories:
        type: hidden

      pageindex:
        columns:
          categories: ''
