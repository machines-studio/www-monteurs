title: Articles
icon: folder
extends:
 - presets/tabs

tabs:
  content:
    columns:
      - width: 1/3
        fields:
          categories: fields/categories

      - width: 2/3
        sections:
          fields:
            fields:
              description:
                  label: Chapô (optionnel)
                  type: writer
          drafts: sections/drafts
          pages: sections/pages
